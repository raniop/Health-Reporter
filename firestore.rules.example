rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // נתוני ניתוח AION – קריאה/כתיבה רק למשתמש המחובר על המסמך שלו
    match /users/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
